version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ejs-viewer
    ports:
      - "5153:5153"
    env_file:
      - .env
      - server/.env
    environment:
      - NODE_ENV=production
      - PORT=5153
    restart: unless-stopped
